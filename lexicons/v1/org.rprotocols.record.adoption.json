{
  "lexicon": 1,
  "id": "org.rprotocols.record.adoption",
  "type": "record",
  "record": {
    "type": "object",
    "description": "Signals that an account adopts (uses) a protocol.",
    "required": ["protocol"],
    "properties": {
      "protocol": { "type": "string", "description": "at:// URI of the protocol record" },
      "note": { "type": "string", "maxLength": 280 },
      "createdAt": { "type": "string", "format": "datetime" }
    }
  }
}
